version: '3'
services:
  docker-factorio:
    build: .
    container_name: docker-factorio
    restart: always
    volumes:
      - ../volumes/factorio:/app/saves
    networks:
      - container-network
networks:
  container-network:
    external: true